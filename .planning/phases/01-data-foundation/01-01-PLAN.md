---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sfdx-project.json
  - force-app/main/default/objects/Invoice/fields/PO_Number__c.field-meta.xml
  - force-app/main/default/objects/Invoice/fields/Portal_Status__c.field-meta.xml
  - force-app/main/default/objects/Invoice/fields/Sold_To_Account__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Payment_Line__c/Invoice_Payment_Line__c.object-meta.xml
  - force-app/main/default/objects/Invoice_Payment_Line__c/fields/Payment__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Payment_Line__c/fields/Invoice__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Payment_Line__c/fields/Payment_Amount__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Payment_Line__c/fields/Short_Pay_Reason__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Payment_Line__c/fields/Comments__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Payment_Line__c/fields/Processing_Fee__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/Invoice_Portal_Config__mdt.object-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Processing_Fee_Percent__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Processing_Fee_Enabled__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/ACH_Enabled__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Credit_Card_Enabled__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Invoices_Per_Page__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/PaymentLink_TTL_Hours__c.field-meta.xml
  - force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Short_Pay_Reasons__c.field-meta.xml
  - force-app/main/default/customMetadata/Invoice_Portal_Config.Default.md-meta.xml
  - force-app/main/default/permissionsets/Invoice_Portal_User.permissionset-meta.xml
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "sfdx-project.json exists with API v66.0 and no namespace"
    - "Custom fields PO_Number__c, Portal_Status__c, Sold_To_Account__c exist on Invoice object as SFDX source XML"
    - "Invoice_Payment_Line__c custom object exists with all 6 fields (Payment__c, Invoice__c, Payment_Amount__c, Short_Pay_Reason__c, Comments__c, Processing_Fee__c)"
    - "Invoice_Portal_Config__mdt CMDT type exists with 7 setting fields and a Default record with correct values"
    - "Invoice_Portal_User permission set grants access to all custom fields and objects (classAccesses deferred to Plan 02 when Apex classes exist)"
  artifacts:
    - path: "sfdx-project.json"
      provides: "SFDX project configuration"
      contains: "sourceApiVersion.*66.0"
    - path: "force-app/main/default/objects/Invoice/fields/PO_Number__c.field-meta.xml"
      provides: "PO Number custom field on Invoice"
      contains: "<type>Text</type>"
    - path: "force-app/main/default/objects/Invoice_Payment_Line__c/Invoice_Payment_Line__c.object-meta.xml"
      provides: "Custom junction object definition"
      contains: "<deploymentStatus>Deployed</deploymentStatus>"
    - path: "force-app/main/default/customMetadata/Invoice_Portal_Config.Default.md-meta.xml"
      provides: "Default CMDT config record"
      contains: "Processing_Fee_Percent__c"
    - path: "force-app/main/default/permissionsets/Invoice_Portal_User.permissionset-meta.xml"
      provides: "Portal user permission set (fields and objects only)"
      contains: "Invoice_Payment_Line__c"
  key_links:
    - from: "permissionsets/Invoice_Portal_User.permissionset-meta.xml"
      to: "objects/Invoice/fields/*.field-meta.xml"
      via: "fieldPermissions references"
      pattern: "Invoice\\.PO_Number__c|Invoice\\.Portal_Status__c|Invoice\\.Sold_To_Account__c"
    - from: "permissionsets/Invoice_Portal_User.permissionset-meta.xml"
      to: "objects/Invoice_Payment_Line__c/"
      via: "objectPermissions and fieldPermissions references"
      pattern: "Invoice_Payment_Line__c"
---

<objective>
Create the SFDX project scaffold and all metadata XML files: custom fields on Invoice, Invoice_Payment_Line__c junction object, Invoice_Portal_Config__mdt Custom Metadata Type with default record, and the Invoice_Portal_User permission set.

Purpose: Establish the complete data model that all Apex services (Plan 02) and downstream UI phases depend on.
Output: Deployable SFDX project with all metadata XML files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@docs/invoice-portal-prd.md (Section 7 for data model, Section 10 for configuration)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SFDX project scaffold and Invoice custom fields</name>
  <files>
    sfdx-project.json,
    force-app/main/default/objects/Invoice/fields/PO_Number__c.field-meta.xml,
    force-app/main/default/objects/Invoice/fields/Portal_Status__c.field-meta.xml,
    force-app/main/default/objects/Invoice/fields/Sold_To_Account__c.field-meta.xml
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 1 for exact XML format of each field type),
    docs/invoice-portal-prd.md (Section 7.2 for custom field specifications)
  </read_first>
  <action>
1. Create `sfdx-project.json` at project root with:
   - `packageDirectories`: `[{"path": "force-app", "default": true}]`
   - `namespace`: `""` (empty string, per D-02)
   - `sourceApiVersion`: `"66.0"` (per D-01)
   - `sfdcLoginUrl`: `"https://login.salesforce.com"`

2. Create directory structure: `force-app/main/default/objects/Invoice/fields/`

3. Create `PO_Number__c.field-meta.xml` -- Text(50) field:
   - `<type>Text</type>`, `<length>50</length>`
   - `<label>PO Number</label>`, `<required>false</required>`
   - `<externalId>false</externalId>`, `<trackHistory>false</trackHistory>`
   - `<description>Customer purchase order reference number</description>`
   - Include XML declaration and `<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">`

4. Create `Portal_Status__c.field-meta.xml` -- Picklist field:
   - `<type>Picklist</type>`, `<required>false</required>`
   - `<label>Portal Status</label>`
   - `<valueSet>` with `<restricted>true</restricted>` and 3 values:
     - `Open` (default=true), `In Progress` (default=false), `Paid` (default=false)
   - `<description>Portal-specific status overlay for invoice display</description>`

5. Create `Sold_To_Account__c.field-meta.xml` -- Lookup(Account) field:
   - `<type>Lookup</type>`, `<referenceTo>Account</referenceTo>`
   - `<relationshipName>SoldToInvoices</relationshipName>`
   - `<relationshipLabel>Sold To Invoices</relationshipLabel>`
   - `<required>false</required>`, `<deleteConstraint>SetNull</deleteConstraint>`
   - `<description>Sold-to account when different from bill-to (BillingAccountId)</description>`

Per D-03: All metadata defined as SFDX source XML. Per D-06: Standard force-app/main/default/ path.
  </action>
  <verify>
    <automated>test -f sfdx-project.json && grep -q '"66.0"' sfdx-project.json && test -f force-app/main/default/objects/Invoice/fields/PO_Number__c.field-meta.xml && test -f force-app/main/default/objects/Invoice/fields/Portal_Status__c.field-meta.xml && test -f force-app/main/default/objects/Invoice/fields/Sold_To_Account__c.field-meta.xml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sfdx-project.json contains `"sourceApiVersion": "66.0"` and `"namespace": ""`
    - PO_Number__c.field-meta.xml contains `<type>Text</type>` and `<length>50</length>`
    - Portal_Status__c.field-meta.xml contains `<type>Picklist</type>` and `<fullName>Open</fullName>` and `<fullName>In Progress</fullName>` and `<fullName>Paid</fullName>`
    - Sold_To_Account__c.field-meta.xml contains `<type>Lookup</type>` and `<referenceTo>Account</referenceTo>`
    - All XML files have `<?xml version="1.0" encoding="UTF-8"?>` declaration
    - All XML files use namespace `xmlns="http://soap.sforce.com/2006/04/metadata"`
  </acceptance_criteria>
  <done>sfdx-project.json exists at project root with v66.0 and no namespace. Three custom field XML files exist under force-app/main/default/objects/Invoice/fields/ with correct types, labels, and metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Create Invoice_Payment_Line__c junction object with all fields</name>
  <files>
    force-app/main/default/objects/Invoice_Payment_Line__c/Invoice_Payment_Line__c.object-meta.xml,
    force-app/main/default/objects/Invoice_Payment_Line__c/fields/Payment__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Payment_Line__c/fields/Invoice__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Payment_Line__c/fields/Payment_Amount__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Payment_Line__c/fields/Short_Pay_Reason__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Payment_Line__c/fields/Comments__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Payment_Line__c/fields/Processing_Fee__c.field-meta.xml
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 2 for object and field XML examples),
    docs/invoice-portal-prd.md (Section 7.3 for Invoice_Payment_Line__c field specifications)
  </read_first>
  <action>
1. Create directory: `force-app/main/default/objects/Invoice_Payment_Line__c/fields/`

2. Create `Invoice_Payment_Line__c.object-meta.xml`:
   - `<label>Invoice Payment Line</label>`
   - `<pluralLabel>Invoice Payment Lines</pluralLabel>`
   - `<nameField>` with `<type>AutoNumber</type>` and `<displayFormat>IPL-{0000}</displayFormat>` and `<label>Invoice Payment Line Name</label>`
   - `<deploymentStatus>Deployed</deploymentStatus>`
   - `<sharingModel>ReadWrite</sharingModel>`
   - `<enableActivities>false</enableActivities>`, `<enableReports>true</enableReports>`, `<enableSearch>true</enableSearch>`, `<enableHistory>false</enableHistory>`
   - `<description>Junction object linking a Payment to individual Invoices with payment-specific metadata (reason codes, comments, processing fee allocation)</description>`

3. Create `Payment__c.field-meta.xml` -- Lookup(Payment):
   - `<type>Lookup</type>`, `<referenceTo>Payment</referenceTo>`
   - `<relationshipName>InvoicePaymentLines</relationshipName>`, `<relationshipLabel>Invoice Payment Lines</relationshipLabel>`
   - `<required>true</required>`, `<deleteConstraint>Restrict</deleteConstraint>`

4. Create `Invoice__c.field-meta.xml` -- Lookup(Invoice):
   - `<type>Lookup</type>`, `<referenceTo>Invoice</referenceTo>`
   - `<relationshipName>InvoicePaymentLines</relationshipName>`, `<relationshipLabel>Invoice Payment Lines</relationshipLabel>`
   - `<required>true</required>`, `<deleteConstraint>SetNull</deleteConstraint>`
   - NOTE: Use SetNull for Invoice__c (not Restrict) per RESEARCH.md Pitfall 4

5. Create `Payment_Amount__c.field-meta.xml` -- Currency:
   - `<type>Currency</type>`, `<precision>18</precision>`, `<scale>2</scale>`, `<required>true</required>`

6. Create `Short_Pay_Reason__c.field-meta.xml` -- Picklist:
   - `<type>Picklist</type>`, `<required>false</required>`
   - Values: `Sales Tax`, `Freight`, `Damaged`, `Pricing`, `Other` (all default=false)
   - `<restricted>true</restricted>`

7. Create `Comments__c.field-meta.xml` -- LongTextArea:
   - `<type>LongTextArea</type>`, `<length>255</length>`, `<visibleLines>3</visibleLines>`
   - `<required>false</required>`

8. Create `Processing_Fee__c.field-meta.xml` -- Currency:
   - `<type>Currency</type>`, `<precision>18</precision>`, `<scale>2</scale>`, `<required>false</required>`

IMPORTANT: Use Lookup relationships (NOT Master-Detail) for both Payment__c and Invoice__c per RESEARCH.md anti-patterns section.
  </action>
  <verify>
    <automated>test -f force-app/main/default/objects/Invoice_Payment_Line__c/Invoice_Payment_Line__c.object-meta.xml && ls force-app/main/default/objects/Invoice_Payment_Line__c/fields/ | wc -l | grep -q "6" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Invoice_Payment_Line__c.object-meta.xml contains `<deploymentStatus>Deployed</deploymentStatus>` and `<displayFormat>IPL-{0000}</displayFormat>`
    - Payment__c.field-meta.xml contains `<referenceTo>Payment</referenceTo>` and `<deleteConstraint>Restrict</deleteConstraint>`
    - Invoice__c.field-meta.xml contains `<referenceTo>Invoice</referenceTo>` and `<deleteConstraint>SetNull</deleteConstraint>`
    - Payment_Amount__c.field-meta.xml contains `<type>Currency</type>` and `<required>true</required>`
    - Short_Pay_Reason__c.field-meta.xml contains `<fullName>Sales Tax</fullName>` and `<fullName>Freight</fullName>` and `<fullName>Damaged</fullName>` and `<fullName>Pricing</fullName>` and `<fullName>Other</fullName>`
    - Comments__c.field-meta.xml contains `<type>LongTextArea</type>` and `<length>255</length>`
    - Processing_Fee__c.field-meta.xml contains `<type>Currency</type>` and `<required>false</required>`
    - Exactly 6 field files exist in the fields/ directory
  </acceptance_criteria>
  <done>Invoice_Payment_Line__c object-meta.xml exists with AutoNumber name field. All 6 field XML files exist with correct types: 2 Lookups (Payment with Restrict, Invoice with SetNull), 2 Currency fields, 1 Picklist with 5 values, 1 LongTextArea.</done>
</task>

<task type="auto">
  <name>Task 3: Create CMDT type, default record, and permission set</name>
  <files>
    force-app/main/default/objects/Invoice_Portal_Config__mdt/Invoice_Portal_Config__mdt.object-meta.xml,
    force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Processing_Fee_Percent__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Processing_Fee_Enabled__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/ACH_Enabled__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Credit_Card_Enabled__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Invoices_Per_Page__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/PaymentLink_TTL_Hours__c.field-meta.xml,
    force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/Short_Pay_Reasons__c.field-meta.xml,
    force-app/main/default/customMetadata/Invoice_Portal_Config.Default.md-meta.xml,
    force-app/main/default/permissionsets/Invoice_Portal_User.permissionset-meta.xml
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 3 for CMDT XML, Pattern 6 for permission set XML),
    docs/invoice-portal-prd.md (Section 10 for configuration settings and defaults)
  </read_first>
  <action>
1. Create directory: `force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/`

2. Create `Invoice_Portal_Config__mdt.object-meta.xml`:
   - `<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">`
   - `<label>Invoice Portal Config</label>`, `<pluralLabel>Invoice Portal Configs</pluralLabel>`
   - `<visibility>Public</visibility>`

3. Create 7 CMDT field files:
   - `Processing_Fee_Percent__c.field-meta.xml`: `<type>Number</type>`, `<precision>5</precision>`, `<scale>2</scale>`, `<label>Processing Fee Percent</label>`
   - `Processing_Fee_Enabled__c.field-meta.xml`: `<type>Checkbox</type>`, `<defaultValue>true</defaultValue>`, `<label>Processing Fee Enabled</label>`
   - `ACH_Enabled__c.field-meta.xml`: `<type>Checkbox</type>`, `<defaultValue>true</defaultValue>`, `<label>ACH Enabled</label>`
   - `Credit_Card_Enabled__c.field-meta.xml`: `<type>Checkbox</type>`, `<defaultValue>true</defaultValue>`, `<label>Credit Card Enabled</label>`
   - `Invoices_Per_Page__c.field-meta.xml`: `<type>Number</type>`, `<precision>4</precision>`, `<scale>0</scale>`, `<label>Invoices Per Page</label>`
   - `PaymentLink_TTL_Hours__c.field-meta.xml`: `<type>Number</type>`, `<precision>5</precision>`, `<scale>0</scale>`, `<label>PaymentLink TTL Hours</label>`
   - `Short_Pay_Reasons__c.field-meta.xml`: `<type>TextArea</type>`, `<label>Short Pay Reasons</label>` (semicolon-delimited string)

   NOTE: CMDT fields use `<CustomField>` with the same namespace but do NOT include `<trackHistory>`, `<externalId>`, or `<required>` tags (CMDT fields have different allowed elements).

4. Create `force-app/main/default/customMetadata/` directory and `Invoice_Portal_Config.Default.md-meta.xml`:
   - `<CustomMetadata xmlns="http://soap.sforce.com/2006/04/metadata" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema">`
   - `<label>Default</label>`, `<protected>false</protected>`
   - Values (each as `<values><field>...<value>...</values>`):
     - `Processing_Fee_Percent__c` = `2.9` (xsi:type="xsd:double")
     - `Processing_Fee_Enabled__c` = `true` (xsi:type="xsd:boolean")
     - `ACH_Enabled__c` = `true` (xsi:type="xsd:boolean")
     - `Credit_Card_Enabled__c` = `true` (xsi:type="xsd:boolean")
     - `Invoices_Per_Page__c` = `20` (xsi:type="xsd:double")
     - `PaymentLink_TTL_Hours__c` = `72` (xsi:type="xsd:double")
     - `Short_Pay_Reasons__c` = `Sales Tax;Freight;Damaged;Pricing;Other` (xsi:type="xsd:string")

5. Create `force-app/main/default/permissionsets/` directory and `Invoice_Portal_User.permissionset-meta.xml`:
   - `<label>Invoice Portal User</label>`
   - `<description>Grants access to Invoice Portal custom fields and objects</description>`
   - `<hasActivationRequired>false</hasActivationRequired>`
   - `<license>Salesforce</license>`
   - fieldPermissions for Invoice.PO_Number__c, Invoice.Portal_Status__c, Invoice.Sold_To_Account__c (all editable=true, readable=true)
   - objectPermissions for Invoice_Payment_Line__c: allowCreate=true, allowDelete=false, allowEdit=true, allowRead=true, modifyAllRecords=false, viewAllRecords=false
   - fieldPermissions for all 6 Invoice_Payment_Line__c fields (all editable=true, readable=true)
   - DO NOT include classAccesses here -- InvoiceService and PaymentService Apex classes do not exist yet (created in Plan 02). classAccesses will be added to this file by Plan 02 after the classes are created.
  </action>
  <verify>
    <automated>test -f force-app/main/default/objects/Invoice_Portal_Config__mdt/Invoice_Portal_Config__mdt.object-meta.xml && ls force-app/main/default/objects/Invoice_Portal_Config__mdt/fields/ | wc -l | grep -q "7" && test -f force-app/main/default/customMetadata/Invoice_Portal_Config.Default.md-meta.xml && test -f force-app/main/default/permissionsets/Invoice_Portal_User.permissionset-meta.xml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Invoice_Portal_Config__mdt.object-meta.xml contains `<visibility>Public</visibility>`
    - Exactly 7 field files in Invoice_Portal_Config__mdt/fields/
    - Invoice_Portal_Config.Default.md-meta.xml contains `<field>Processing_Fee_Percent__c</field>` and `2.9`
    - Invoice_Portal_Config.Default.md-meta.xml contains `<field>Short_Pay_Reasons__c</field>` and `Sales Tax;Freight;Damaged;Pricing;Other`
    - Invoice_Portal_Config.Default.md-meta.xml contains `<field>Invoices_Per_Page__c</field>` and `20`
    - Invoice_Portal_User.permissionset-meta.xml contains `<field>Invoice.PO_Number__c</field>`
    - Invoice_Portal_User.permissionset-meta.xml contains `<object>Invoice_Payment_Line__c</object>`
    - Invoice_Portal_User.permissionset-meta.xml does NOT contain `<apexClass>` (classAccesses deferred to Plan 02)
  </acceptance_criteria>
  <done>CMDT type definition exists with 7 fields. Default record has all config values matching PRD Section 10 defaults. Permission set grants field access for all 3 Invoice custom fields and CRUD on Invoice_Payment_Line__c with all 6 fields. No classAccesses included (deferred to Plan 02).</done>
</task>

</tasks>

<verification>
1. `sfdx-project.json` exists with sourceApiVersion "66.0" and empty namespace
2. 3 field XML files exist under `objects/Invoice/fields/`
3. 1 object XML + 6 field XMLs exist under `objects/Invoice_Payment_Line__c/`
4. 1 object XML + 7 field XMLs exist under `objects/Invoice_Portal_Config__mdt/`
5. 1 CMDT default record exists under `customMetadata/`
6. 1 permission set exists under `permissionsets/` with field and object permissions only (no classAccesses)
7. All XML files have correct declaration and namespace
</verification>

<success_criteria>
- Total of 22 metadata files created in correct SFDX directory structure
- sfdx-project.json pinned to API v66.0 with no namespace (per D-01, D-02)
- All metadata is SFDX source XML (per D-03, D-05, D-06)
- Invoice custom fields match PRD Section 7.2 specifications exactly
- Invoice_Payment_Line__c fields match PRD Section 7.3 specifications exactly
- CMDT default values match PRD Section 10 defaults exactly
- Permission set covers all custom fields and objects (classAccesses deferred to Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
